# Phase 3: ai drafting — Ledger

<!-- append-only; one line per verified task; server appends, never rewrites -->

- [x] T1–T4 — AI drafting complete: SDK + config + encrypted BYO-key resolution (team > instance), draft/improve/status endpoints with structured outputs + hourly rate caps + redaction, wizard AI path + feedback + Team Settings key panel, mock-Anthropic tests (213 vitest), e2e draft journey (3 specs), production-container smoke with mock on docker network, env docs — commits 0f9fb44..88a3f2f — 16 closed 2026-07-31
