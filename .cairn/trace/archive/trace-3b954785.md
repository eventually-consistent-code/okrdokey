---
status: resolved
issue: 19
created: 2026-07-31
resolved: 2026-07-31
---
# Trace: v2 P3 verify failed adversarial pass: (1) draft-KR one-retry promise doesn't cover throw-class invalid output — malformed/truncated model JSON makes beta.messages.parse throw AnthropicError (base class, not APIError) which skips the retry and falls to the generic 502; (2) improve-kr can 500 instead of a typed error — improveWrapperSchema allows critique: [] but the shared response schema requires min(1), failing response serialization; (3) claimed test coverage missing: 529/RateLimitError mappings never exercised, team 30/hr limit untested, improve-kr 409/429 untested, /ai/status feature-off + no-key states untested API-side, non-member 404 untested, retry test doesn't count attempts.

## evidence — 2026-07-31
Adversarial subagent evidence: SDK helpers (node_modules/@anthropic-ai/sdk/helpers/beta/zod.js:27-29, lib/beta-parser.js:69-77) throw AnthropicError on malformed JSON / wrapper-schema mismatch — parse never returns parsed_output null for that class. routes.ts:210-212 retry only reachable when attempt() resolves; AnthropicError is APIError's BASE class so it hits the generic fallback (routes.ts:88) with zero retries. Concrete: max_tokens 1024 truncates 3-suggestion JSON → stop_reason max_tokens → JSON.parse throws → immediate generic 502. Second defect: improveWrapperSchema (routes.ts:37-40) lacks .min on critique; shared improveKrResponseSchema requires min(1) → Fastify response serialization 500 on empty critique. Third: ai.test.ts declares 'overloaded' mock mode but never assigns it; no RateLimitError mode; team 30/hr, improve-kr 409/429, API-side /ai/status states, non-member 404, retry attempt-count all unasserted.

## hypothesis — 2026-07-31
Fix plan: (1) attempt() catches AnthropicError-but-not-APIError (parse/truncation class) and returns [] — retry loop then covers it; APIError subclasses (401/429/529) still propagate to mapAiError. Same guard on improve-kr parse → typed 502 'could not produce usable feedback'. (2) improveWrapperSchema critique gets .min(1).max(5) so an empty-critique response fails parse (→ typed 502) instead of response serialization (→ 500). (3) Test additions: mock modes 'rate-limited' (429) + assign 'overloaded'; garbage-JSON mode asserting retry-then-502 with attempt count; team 30/hr exhaustion via 4 users; improve-kr 409 + 429; API-side /ai/status disabled-404 + enabled-no-key; non-member draft 404. Plus two one-line hardenings from minor findings: ai-key-card hides on any non-data status (not just null), improve prompt omits the numbers segment unless both baseline AND target present.

## test — 2026-07-31
Fixes applied in f774e0a. New mock modes (garbage JSON with stop_reason max_tokens, upstream 429 without retry-after, empty-critique) plus a messages-hit counter prove: tasks-mode AND garbage-mode both retry exactly once (messagesHits === 2) then 502 'could not produce'; 529 → 502 'unavailable right now'; upstream 429 → 502 'rate limit'; empty critique → typed 502 'usable feedback' (not 500); team 30/hr exhausts across 4 users while the same admin still drafts on a personal objective; improve-kr 409 and 429; /ai/status enabled:false with no key and 404 with AI_FEATURES off; non-member 404 on team objective and member 404 on another user's personal objective. Full gates re-run post-fix: 220 vitest, 3 Playwright e2e, rebuilt production-container smoke (draft + improve + log-leak scan) all green; lint + typecheck clean. One test-side gotcha logged: SDK honors retry-after literally, so the mock's 429 must not send it or the test sleeps 60s.

## verdict — 2026-07-31
Root causes confirmed and fixed: (1) SDK parse failures throw AnthropicError (base class), bypassing both the retry and the typed mapping — now classified as invalid output and retried once; (2) wrapper/response schema mismatch on critique minimums turned a model quirk into a 500 — wrapper now matches the response contract so it degrades to a typed 502; (3) claimed-but-missing coverage was real, now closed with 12 additional assertions across error mappings, both rate scopes, status states, and membership 404s. All gates green post-fix (220 vitest / 3 e2e / container smoke on rebuilt image).

## resolution — 2026-07-31
Fixed in f774e0a: parse-throw invalid output now covered by the one-retry contract, empty-critique degrades to a typed 502 instead of a serialization 500, and the claimed test coverage gaps are closed (220 vitest, 3 e2e, rebuilt container smoke all green).
