########################
# Compose by John Reed #
# 2026-07-30           #
########################
#
# The whole deploy story: docker compose up. Data lives in ./data —
# backup = copy one file.

services:
  okrdokey:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./data:/data
    restart: unless-stopped
    # AI drafting (optional): set an instance-wide Anthropic key here, or
    # let each team bring its own in Team Settings. AI_FEATURES=off kills
    # every AI route and UI affordance.
    # environment:
    #   ANTHROPIC_API_KEY: sk-ant-...
    #   AI_MODEL: claude-opus-4-8
    #   AI_FEATURES: on
    # Email digests (optional): both SMTP_HOST and SMTP_FROM, or neither.
    #   SMTP_HOST: mail.example.com
    #   SMTP_FROM: okrdokey@example.com
    #   SMTP_PORT: "587"
    #   SMTP_USER: okrdokey
    #   SMTP_PASS: change-me
